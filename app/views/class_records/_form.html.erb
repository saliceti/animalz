<%= form_with(model: class_record, local: true) do |form| %>
  <% if class_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_record.errors.count, "error") %> prohibited this class_record from being saved:</h2>

      <ul>
      <% class_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :phylum_record_id %>
    <%= form.text_field :phylum_record_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
