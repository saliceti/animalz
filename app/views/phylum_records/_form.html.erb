<%= form_with(model: phylum_record, local: true) do |form| %>
  <% if phylum_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phylum_record.errors.count, "error") %> prohibited this phylum_record from being saved:</h2>

      <ul>
      <% phylum_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
